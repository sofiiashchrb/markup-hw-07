# Звіт про помилки в проекті markup-hw-07

## HTML Валідність

### index.html

1. **Рядок 16**: `<meta charset="UTF-8" />` має бути першим тегом в `<head>`, перед іншими тегами (зараз він після `<link>` тегів)
2. **Рядок 17**: Помилка в атрибуті `content`: `content="width=\, initial-scale=1.0"` - зайвий символ `\` перед комою. Має бути: `content="width=device-width, initial-scale=1.0"`
3. **Рядок 21**: HTML коментар `<!-- qwe -->` - має бути видалений
4. **Рядок 2**: Атрибут `lang="en"` встановлений на `en`, але контент сторінки українською мовою. Має бути `lang="uk"`

### portfolio.html

1. **Рядок 2**: Атрибут `lang="en"` встановлений на `en`, але контент сторінки українською мовою. Має бути `lang="uk"`

---

## BEM Методологія - Помилки в іменуванні

### B1-B2: Помилки в іменуванні блоків та елементів

1. **portfolio.html, рядок 28**: `class="header___nav"` - три підкреслення замість двох. Має бути `header__nav`
2. **portfolio.html, рядок 96; index.html**: Клас `card` використовується як окремий блок, але він є частиною секції `projects`. Має бути `projects__card` або `projects-item`
3. **portfolio.html, рядок 93**: `projects__title--hidden` - модифікатор на елементі, але назва елемента некоректна. Має бути `projects__title projects__title--hidden` або просто приховати через CSS

### B3: Помилки в іменуванні модифікаторів

1. **portfolio.html, рядок 287; index.html, рядок 389**: `footer__logo--right` - модифікатор на елементі `footer__logo`, але в HTML він застосований до `<span>` всередині логотипу. Має бути `logo__accent` або `footer__logo-accent`
2. **portfolio.html, рядок 318; index.html, рядок 420**: `social-links__item--footer` - модифікатор має бути на блоці `social-links`, а не на елементі. Правильніше: `social-links--footer` на `<ul>` та `social-links__item` на `<li>`

### B4: Помилки в іменуванні міксінів

1. **portfolio.html, рядок 24; index.html, рядок 26**: `header__logo logo` - міксін використаний правильно
2. **portfolio.html, рядок 44; index.html, рядок 46**: `header__contacts contacts` - міксін використаний правильно
3. **portfolio.html, рядок 293; index.html, рядок 395**: `footer__coordination coordination` - міксін використаний правильно

### B5: Неправильні класи (не BEM)

1. **index.html, рядок 77**: `advantages__list list` - клас `list` не є BEM класом, має бути видалений
2. **portfolio.html, рядок 284, 315, 348; index.html, рядок 386, 417, 450**: Класи `left-part`, `middle-part`, `right-part` - не відповідають BEM методології. Мають бути `footer__left`, `footer__middle`, `footer__right` або подібні
3. **portfolio.html, рядок 316; index.html, рядок 418**: `footer__join join` - клас `join` не є BEM класом, має бути видалений
4. **portfolio.html, рядок 349; index.html, рядок 451**: `footer__subscribe subscribe` - клас `subscribe` не є BEM класом, має бути видалений
5. **portfolio.html, рядок 360; index.html, рядок 462**: `icon-send` - не BEM клас. Має бути `subscribe__icon` або `subscribe__icon-send`
6. **sass/components/_team.scss, рядок 55**: `.social-links` - окремий блок, але використовується всередині `.team`. Має бути `team__social-links` або міксін
7. **sass/components/portfolio/_projects.scss, рядок 14**: `.card` - окремий блок, але використовується всередині `.projects`. Має бути `projects__card`

---

## SASS Структура - Помилки

### A3-A4: Структура файлів SASS

✅ **Пройдено**: Папка `sass` створена, файл `main.scss` існує та імпортує всі фрагменти

### A5: Змінні

✅ **Пройдено**: Файл `sass/utils/_variables.scss` існує та містить CSS змінні

### A6: Компоненти

✅ **Пройдено**: Для кожного компонента створений окремий файл-фрагмент

### A7: Підключення стилів

✅ **Пройдено**: В обох HTML файлах підключений `main.min.css`

---

## SASS Вкладеність - Помилки

### C1-C2: Максимальна вкладеність 2 рівні

1. **sass/components/_backdrop-modal.scss, рядок 17**: Вкладеність 3 рівні:
   ```scss
   .backdrop.is-hidden {
     .modal {  // Рівень 2
       transform: ...;  // Рівень 3 - ПОМИЛКА
     }
   }
   ```
   Має бути винесено `.modal` на рівень 1 або використати `&` оператор

2. **sass/components/_backdrop-modal.scss, рядок 99-100**: Вкладеність 3 рівні:
   ```scss
   .label__icon {
     .label:hover &,  // Рівень 2
       .label:focus & {  // Рівень 3 - ПОМИЛКА
         border-color: ...;
       }
   }
   ```
   Це використання `&` для зміни контексту, але все одно перевищує 2 рівні вкладеності

3. **sass/components/_backdrop-modal.scss, рядок 182**: Вкладеність 3 рівні:
   ```scss
   .checkbox__icon {
     .checkbox__click:checked + & {  // Рівень 2
       background-image: ...;  // Рівень 3 - ПОМИЛКА
       display: flex;  // Рівень 3
       ...
     }
   }
   ```

4. **sass/components/_logo.scss, рядок 18**: Вкладеність 3 рівні:
   ```scss
   .footer__logo {
     &--right {  // Рівень 2
       color: ...;  // Рівень 3 - ПОМИЛКА (хоча це технічно правильно, але перевищує ліміт)
     }
   }
   ```
   Це правильне використання BEM модифікатора, але перевищує ліміт в 2 рівні

### C3: Оператор конкатенації (&)

✅ **Пройдено**: Оператор `&` використовується для псевдокласів та псевдоелементів в більшості файлів

⚠️ **Проблема**: Деякі використання `&` створюють глибоку вкладеність (див. вище)

---

## Інші помилки

### SASS Валідність

1. **sass/components/_backdrop-modal.scss, рядок 7**: Використання `rgba(51, 51, 51, 0.5019607843)` - краще використати коротший формат `rgba(51, 51, 51, 0.5)`
2. **sass/components/_about.scss, рядок 39**: Використання `#2f303acc` замість `rgba(47, 48, 58, 0.8)` - краще використати rgba для читабельності
3. **sass/components/portfolio/_projects.scss, рядок 47**: Використання `#2196f3e6` замість `rgba(33, 150, 243, 0.9)` - краще використати rgba

### Коментарі в коді

1. **sass/components/_team.scss**: Закоментований код (рядки 57-59, 69-71, 77-79) - має бути видалений
2. **sass/components/_footer.scss**: Закоментований код (рядки 57-59, 67-69, 120-122) - має бути видалений
3. **sass/components/_clients.scss**: Закоментований код (рядки 25-27) - має бути видалений
4. **sass/components/portfolio/_filters.scss**: Закоментований код (рядки 7-9) - має бути видалений
5. **sass/components/_backdrop-modal.scss**: Закоментований код (рядки 73, 142-148) - має бути видалений

### Структура HTML

1. **portfolio.html, рядок 93**: `<h1 class="projects__title--hidden">projects</h1>` - заголовок має бути українською мовою або видалений, якщо він прихований
2. **index.html, рядок 77**: Клас `list` на `<ul>` не використовується в SASS файлах - має бути видалений

### Проблеми з BEM структурою в SASS

1. **sass/components/_footer.scss, рядок 15**: `.coordination__address` - клас `coordination` використовується як блок, але він є частиною `footer`. Має бути `footer__address` або міксін
2. **sass/components/_footer.scss, рядок 32**: `.coordination__contact-nav` - аналогічно, має бути `footer__contact` або подібне
3. **sass/components/_footer.scss, рядок 52**: `.social-links__item--footer` - модифікатор на елементі, але краще використати міксін або окремий блок

---

## Підсумок помилок

### Критичні помилки (потрібно виправити):

1. ❌ HTML валідність: неправильний порядок meta тегів, помилка в viewport
2. ❌ BEM: `header___nav` (три підкреслення)
3. ❌ BEM: класи `left-part`, `middle-part`, `right-part` не відповідають BEM
4. ❌ BEM: класи `list`, `join`, `subscribe`, `icon-send` не BEM
5. ❌ SASS вкладеність: перевищення ліміту в 2 рівні в 4 місцях

### Помилки середньої важливості:

1. ⚠️ HTML: неправильна мова в `lang` атрибуті
2. ⚠️ HTML: залишковий коментар `<!-- qwe -->`
3. ⚠️ BEM: неправильне використання модифікаторів
4. ⚠️ SASS: закоментований код

### Дрібні помилки:

1. 💡 SASS: використання hex з альфа-каналом замість rgba
2. 💡 HTML: прихований заголовок з англійським текстом

---

## Рекомендації для виправлення

1. Виправити порядок meta тегів в `index.html`
2. Виправити помилку в viewport meta тезі
3. Замінити всі не-BEM класи на правильні BEM класи
4. Зменшити вкладеність SASS до максимум 2 рівнів
5. Видалити весь закоментований код
6. Змінити `lang="en"` на `lang="uk"` в обох HTML файлах
7. Видалити HTML коментар `<!-- qwe -->`

